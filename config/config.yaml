server:
  port: 8080
  mode: debug   # debug, release, test

database:
  host: localhost
  port: 5432
  user: gouser
  password: gopassword
  name: godb
  sslmode: disable

redis:
  addr: localhost:6379
  password: ""
  db: 0

jwt:
  secret_key: "your-secret-key-change-in-production"
  access_expiry: 30     # 분
  refresh_expiry: 168   # 시간 (7일)

pagination:
  default_size: 10
  max_size: 100
